
# Using script commands
#
  
var report-home = /usr/local/parasoft/loadtest/report
var minutes = 3
  
var scenario = "Linear Increase"
var base = /usr/local/parasoft/loadtest
var category = Accuracy
  
var test-name = ParabankServicesLoadTests
open ${base}/${test-name}.lt
loadtest -minutes ${minutes} -allReports ${report-home}/%d/${category}/${test-name} ${scenario}